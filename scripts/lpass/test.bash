#!/bin/bash -eu

# CREATE
# echo "delphi config" | lpass add --notes --non-interactive "Shared-공유/env/env.part.html"

# UPDATE
# echo "hello world" | lpass edit --notes --non-interactive "$LPASS_ID_SIMR"


# VIEW
# lpass show "$LPASS_ID_SIMR" | grep -v "URL:" | sed 's/^Notes: //g'